<%= @quiz.id %>
<%= image_tag @quiz.photo.url %>
<% @quiz.choices.each do |choice| %>
  <%= link_to Prefecture.find(choice.prefecture_id).name, quiz_result_path(@quiz, answer_prefecture_id: choice.prefecture_id) %>
<% end %>
